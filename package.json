{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "demo:db": "pnpm --filter @workspace/db run push && pnpm --filter @workspace/db run seed",
    "demo:api": "DEMO_MODE=true pnpm --filter @workspace/api-server run dev",
    "demo:web": "PORT=24478 BASE_PATH=/ pnpm --filter @workspace/walker-creek run dev",
    "build:netlify": "pnpm --filter @workspace/walker-creek run build"
  },
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "prettier": "^3.8.4",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "drizzle-zod": "^0.8.3",
    "stripe": "^22.3.0",
    "stripe-replit-sync": "^1.0.0",
    "zod": "catalog:"
  }
}
